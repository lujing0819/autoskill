# 单词计数技能

## 功能描述
读取一个文本文件，统计其中每个单词出现的次数，并将结果写入另一个文件。

## 输入参数
- `input_file` (str): 需要读取的文本文件路径
- `output_file` (str): 结果输出文件路径

## 输出
- 将单词及其出现次数写入输出文件，格式为 "单词: 次数"，每行一个单词

## 使用场景
适用于文本分析、词频统计等场景

## 注意事项
- 自动处理大小写（统一转为小写）
- 自动去除标点符号
- 支持 UTF-8 编码
